{
  "name": "lightning.sivothajan.me",
  "version": "1.0.1",
  "author": "Sivothayan",
  "license": "MIT",
  "description": "custom domain lightning address support for binance lightning nrtwork with the help of Binance API, Supabase and Vercel Compute",
  "type": "module",
  "scripts": {
    "dev": "nodemon api/app.js",
    "format": "prettier --write .",
    "export": "npm run export:bolt11:decode",
    "export:bolt11:decode": "npx esbuild lib/bolt11.decode.js --bundle --outfile=api/bolt11.js --minify --platform=node"
  },
  "dependencies": {
    "esbuild": "^0.25.8",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
